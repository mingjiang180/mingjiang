name: hw Network Connectivity Debug

on:
  # 允许在 GitHub 页面上手动触发运行
  workflow_dispatch:
  # 如果需要，可以取消注释下面两行，让它在推送到 main 分支时自动触发
  # push:
  #   branches: [ "main" ]

jobs:
  network-test:
    runs-on: hw-test-v1
    # 指定运行环境的容器镜像
    container:
      image: harbor.baai.ac.cn/baai-infra/actions-runner:ubuntu-22.04-arm64
      # 如果你的 Harbor 镜像仓库需要登录凭证，请取消下面三行的注释，并在 GitHub Repo Settings -> Secrets 中配置
      # credentials:
      #   username: ${{ secrets.HARBOR_USERNAME }}
      #   password: ${{ secrets.HARBOR_PASSWORD }}
    steps:
      - name: 保持容器运行 (睡眠 30 分钟)
        run: |
          echo "=================================================="
          echo "💤 诊断已完成，容器即将进入睡眠模式，保持运行 30 分钟..."
          echo "⏳ 你可以在此期间手动连接到容器进行进一步的排查。"
          echo "⏰ 结束时间预计：$(date -d '+30 minutes' '+%Y-%m-%d %H:%M:%S')"
          echo "=================================================="
          sleep 3600 # 30 分钟 (30 * 60 秒)
          echo "💤 睡眠结束，工作流即将关闭。"
